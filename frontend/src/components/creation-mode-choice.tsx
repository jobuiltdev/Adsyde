"use client";
import Link from "next/link";import {useEffect} from "react";import {useRouter} from "next/navigation";
export function CreationModeChoice({projectId}:{projectId:string}){const router=useRouter();useEffect(()=>{const mode=localStorage.getItem("adsyde.defaultCreationMode");if(mode==="guided"||mode==="prompt")router.replace(`/app/projects/${projectId}/create?mode=${mode}`)},[projectId,router]);return <main className="page"><div className="page-head"><div><p className="eyebrow">Create Ad</p><h1>How do you want to begin?</h1><p>Both paths keep the final prompt in your hands.</p></div></div><div className="grid"><Link className="card card-link project-card" href={`/app/projects/${projectId}/create?mode=guided`}><p className="eyebrow">Guided Builder</p><h2>Structure the ad with Adsyde</h2><p>Describe the product, audience, offer, and style. Review every part before generating.</p></Link><Link className="card card-link project-card" href={`/app/projects/${projectId}/create?mode=prompt`}><p className="eyebrow">Prompt Studio</p><h2>Start from your own prompt</h2><p>Write and control the exact creative direction yourself.</p></Link></div></main>}
