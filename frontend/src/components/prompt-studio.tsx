"use client";
import { FormEvent,useCallback,useEffect,useState } from "react"; import { useRouter } from "next/navigation"; import { assetsApi,generationsApi,projectsApi } from "@/lib/api/resources"; import type { Asset,Project } from "@/lib/types"; import { effectivePrompt,type PromptMode } from "@/lib/prompt"; import { Button,Notice,Skeleton,Textarea } from "./ui"; import { PrivateImage } from "./private-media";
export function PromptStudio({projectId}:{projectId:string}){const [project,setProject]=useState<Project|null>(null);const [assets,setAssets]=useState<Asset[]>([]);const [mode,setMode]=useState<PromptMode>("exact");const [prompt,setPrompt]=useState("");const [ratio,setRatio]=useState<"9:16"|"1:1"|"16:9">("9:16");const [duration,setDuration]=useState(10);const [selected,setSelected]=useState<string[]>([]);const [busy,setBusy]=useState(false);const [error,setError]=useState("");const router=useRouter();useEffect(()=>{Promise.all([projectsApi.get(projectId),assetsApi.list(projectId)]).then(([p,a])=>{setProject(p);setAssets(a.results)}).catch(e=>setError(e.message))},[projectId]);const toggle=(id:string)=>setSelected(s=>s.includes(id)?s.filter(x=>x!==id):[...s,id]);async function submit(e:FormEvent){e.preventDefault();if(!prompt.trim()){setError("Write a prompt before generating.");return}setBusy(true);setError("");try{const generation=await generationsApi.create(projectId,{prompt:effectivePrompt(prompt,mode),aspect_ratio:ratio,duration_seconds:duration,model:"mock-standard"});router.push(`/app/projects/${projectId}/generations/${generation.id}`)}catch(err){setError(err instanceof Error?err.message:"Generation could not be started.");setBusy(false)}}const mediaLoad=useCallback((id:string)=>assetsApi.content(projectId,id),[projectId]);if(!project&&!error)return <main className="page"><Skeleton lines={6}/></main>;return <main className="page"><div className="page-head"><div><p className="eyebrow">Prompt Studio · {project?.name}</p><h1>Direct the idea.</h1><p>Your words stay under your control.</p></div></div>{error&&<Notice kind="error">{error}</Notice>}<form className="studio" onSubmit={submit}><section className="studio-main"><Textarea label="Describe the video you want to create" value={prompt} onChange={e=>setPrompt(e.target.value)} maxLength={4000} hint={`${prompt.length}/4000 characters`}/><div className="section"><h2>How should Adsyde use your prompt?</h2><div className="choice-grid"><button type="button" className={`choice ${mode==="exact"?"selected":""}`} onClick={()=>setMode("exact")} aria-pressed={mode==="exact"}><strong>Use exactly as written</strong><small>No additions or creative rewriting.</small></button><button type="button" className={`choice ${mode==="enhance"?"selected":""}`} onClick={()=>setMode("enhance")} aria-pressed={mode==="enhance"}><strong>Enhance my prompt</strong><small>Adds a transparent, deterministic creative direction in this development version.</small></button></div>{mode==="enhance"&&<Notice>Enhancement is a local development aid, not an AI service. Your original text remains visible above.</Notice>}</div><div className="section"><h2>Format</h2><div className="ratio-row">{(["9:16","1:1","16:9"] as const).map(r=><button type="button" key={r} className={`ratio ${ratio===r?"selected":""}`} onClick={()=>setRatio(r)} aria-pressed={ratio===r}>{r}</button>)}</div></div><div className="section"><h2>Duration</h2><div className="duration-row">{[5,10,15,20].map(d=><Button type="button" variant={duration===d?"primary":"secondary"} key={d} onClick={()=>setDuration(d)} aria-pressed={duration===d}>{d}s</Button>)}</div></div>{assets.length>0&&<div className="section"><h2>Reference assets <small className="muted">optional context</small></h2><p className="muted">Selections are saved in this editing session only; provider reference inputs are deferred.</p><div className="asset-grid">{assets.map(a=><label className="asset-choice" key={a.id}><input type="checkbox" checked={selected.includes(a.id)} onChange={()=>toggle(a.id)}/><PrivateImage load={()=>mediaLoad(a.id)} alt=""/><span>{a.original_filename}</span></label>)}</div></div>}</section><aside className="studio-side"><div className="card summary"><p className="eyebrow">Generation setup</p><h2>{ratio} video</h2><p>{duration} seconds · Development model</p><p>{mode==="exact"?"Prompt stays exactly as written.":"Local enhancement is active."}</p><p>{selected.length} reference{selected.length===1?"":"s"} selected</p><Button style={{width:"100%"}} disabled={busy||!prompt.trim()}>{busy?"Queueing generation…":"Generate video"}</Button><small className="muted">Submitting once is enough. We’ll track uncertain jobs safely.</small></div></aside></form></main>}
