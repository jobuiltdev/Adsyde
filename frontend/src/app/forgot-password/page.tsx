"use client";
import { FormEvent,useState } from "react"; import Link from "next/link"; import { AuthShell } from "@/components/auth-shell"; import { Button,Field,Notice } from "@/components/ui"; import { authApi } from "@/lib/api/resources";
export default function ForgotPage(){const [message,setMessage]=useState("");const [error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");try{setMessage((await authApi.forgot(String(new FormData(e.currentTarget).get("email")))).detail)}catch(err){setError(err instanceof Error?err.message:"Request failed.")}}return <AuthShell><p className="eyebrow">Account recovery</p><h1>Reset your password</h1><p>We’ll send instructions if the account is eligible.</p>{message&&<Notice kind="success">{message}</Notice>}{error&&<Notice kind="error">{error}</Notice>}<form className="form-stack" onSubmit={submit}><Field label="Email" name="email" type="email" required/><Button>Send reset link</Button></form><p><Link href="/login">Back to sign in</Link></p></AuthShell>}
