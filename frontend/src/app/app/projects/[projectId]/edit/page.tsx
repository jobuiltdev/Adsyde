"use client";
import { use,useEffect,useState } from "react"; import { ProjectForm } from "@/components/project-form"; import { Notice,Skeleton } from "@/components/ui"; import { projectsApi } from "@/lib/api/resources"; import type { Project } from "@/lib/types";
export default function EditProject({params}:{params:Promise<{projectId:string}>}){const {projectId}=use(params);const [project,setProject]=useState<Project|null>(null);const [error,setError]=useState("");useEffect(()=>{projectsApi.get(projectId).then(setProject).catch(e=>setError(e.message))},[projectId]);return <main className="page"><div className="page-head"><div><p className="eyebrow">Project settings</p><h1>Edit project</h1></div></div>{error?<Notice kind="error">{error}</Notice>:project?<ProjectForm project={project}/>:<Skeleton/>}</main>}
