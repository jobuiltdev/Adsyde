"use client";
import { FormEvent,useState } from "react"; import Link from "next/link"; import { AuthShell } from "@/components/auth-shell"; import { Button,Field,Notice } from "@/components/ui"; import { authApi } from "@/lib/api/resources";
export default function RegisterPage(){const [message,setMessage]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);if(form.get("password")!==form.get("password_confirm")){setError("Passwords do not match.");return}setBusy(true);setError("");try{const r=await authApi.register(String(form.get("email")),String(form.get("password")),String(form.get("password_confirm")));setMessage(r.detail)}catch(err){setError(err instanceof Error?err.message:"Registration failed.")}finally{setBusy(false)}}return <AuthShell><p className="eyebrow">Start creating</p><h1>Create your account</h1><p>One focused workspace for your video ads.</p>{message?<><Notice kind="success">{message}</Notice><Link className="button primary" href="/login">Continue to sign in</Link></>:<form className="form-stack" onSubmit={submit}>{error&&<Notice kind="error">{error}</Notice>}<Field label="Email" name="email" type="email" required/><Field label="Password" name="password" type="password" minLength={8} required/><Field label="Confirm password" name="password_confirm" type="password" required/><Button disabled={busy}>{busy?"Creating account…":"Create account"}</Button></form>}<p className="muted">Already have an account? <Link href="/login">Sign in</Link></p></AuthShell>}
